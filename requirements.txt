# Suika AI Challenge - Python Dependencies
# =========================================
# Install: pip install -r requirements.txt
# For full install (render + dev): pip install -e ".[all]"

# Core dependencies
pymunk>=6.6.0               # 2D physics engine (Chipmunk2D bindings)
gymnasium>=0.29.0           # RL environment interface
numpy>=1.24.0               # Numerical arrays
pyyaml>=6.0                 # Config file parsing

# Rendering (optional, for human play and visualization)
pygame>=2.5.0               # Graphics and input handling

# Development (testing)
pytest>=7.0.0               # Test framework
pytest-cov>=4.0.0           # Coverage reporting
